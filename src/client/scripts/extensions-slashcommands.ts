// Re-export shim for the legacy global script `extensions-slashcommands.ts`.
// Real implementation moved to `app/systems/extensions/slashcommands.ts` (client-monolith-decomposition).
export * from "../app/systems/extensions/slashcommands";
